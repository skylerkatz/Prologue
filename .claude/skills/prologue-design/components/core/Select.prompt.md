Labeled dropdown for the toolbar's Base/Branch pickers; option text is mono (branch names are code).

```jsx
<Select label="Base" value="origin/production" options={["origin/production","main"]} />
```
