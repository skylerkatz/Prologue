Segmented control for the diff's working-tree mode; the selected segment is white with an orange dot (mockup spec — not a filled blue segment).

```jsx
<ModeToggle value="committed" onChange={setMode} />
```
